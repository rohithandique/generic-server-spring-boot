spring:
  application:
    name: "generic-server-spring-boot"
  profiles:
    active: "local"
  jmx:
    enabled: true
  datasource:
    url: "jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres?user=postgres.hwnzjlovbrecaizjoqsn&password=${DB_PASSWORD}"
    driver-class-name: "org.postgresql.Driver"
    hikari:
      pool-name: "EnterprisePostgresPool"
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: "SELECT 1"

server:
  port: 8080

springdoc:
  show-actuator: true
  swagger-ui:
    path: "/swagger-ui"
